language:
  - cpp

before_script:
  - sudo apt-get update -qq
  - sudo apt-get install libboost-all-dev libcrypto++-dev libcrypto++-doc libcrypto++-utils
  - cd test
  - mkdir lib && cd lib
  - git clone https://github.com/google/googletest.git && mv googletest gtest
#  - mkdir cryptopp && cd cryptopp
#  - wget https://cryptopp.com/cryptopp610.zip
#  - unzip cryptopp610.zip
#  - make all -j8
#  - sudo make install
#  - sudo cp /usr/local/lib/libcryptopp.so /usr/lib
#  - cd ../../..
  - cd ../..
  - mkdir ci_test
  - cd ci_test
  - cmake ..
  - make -j8

script:
  - ./test/Rockarbon_Test
